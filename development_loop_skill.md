# 開発ループ実行スキル (Development Loop Skill)

このファイルは、AI（Antigravity）が自律的に1回の開発ループ（テストプレイ、詳細レビュー、実装計画、サブエージェントとの設計・コード議論、実際のコーディング、テスト検証）を高品質に実行するためのスキル（指示書）です。

本スキルを使用する際は、このファイルを `IsSkillFile = true` で読み込み、記載された各フェーズの手順を順番に実行してください。

---

## 🛠 前提条件と環境操作コマンド

ゲームのビルドおよび動作検証は、以下のコマンドや手段を用いて行います。

- **Webサーバーの起動 (バックグラウンドタスク)**:
  `python serve.py` を実行して `http://localhost:8000` でゲームをホストします。
- **自動テストプレイ (Puppeteer) の実行**:
  `node test_browser.js` を実行して、ブラウザ上でのゲーム起動テストとスクリーンショットの取得を行います。
- **Godot Webエクスポート (ビルド更新)**:
  `godot --headless --export-release "Web" build/web/index.html` (または同等のエクスポートコマンド) を用いて、GDScript の変更をWebビルドへ反映させます。

---

## 🔄 開発ループの5フェーズ

### 📋 フェーズ 1: テストプレイと詳細レビューの作成
目的: ゲームを実際に動かし、手触り（ジュース感）、バグ、仕様の不整合を洗い出します。

1. **テストサーバーの起動**:
   `run_command` を使用して `python serve.py` を起動し、バックグラウンドでサーバーを待機させます。
2. **自動テスト/スクリーンショットの実行**:
   `run_command` で `node test_browser.js` を実行し、コンソールログや生成されたスクリーンショット（`screenshot_day1.png` など）を確認します。
3. **詳細レビューの作成**:
   テストプレイ結果をまとめ、[playtest_review.md](playtest_review.md)（新規作成または更新）に記述します。
   - **確認項目**: 挙動バグ、UIの視認性、アニメーションのテンポ、GDD仕様との整合性、ジュース感（手触りの良さ）。

---

### 📝 フェーズ 2: 実装計画 (implementation_plan.md) の作成
目的: レビューで判明した課題に対し、具体的な修正方針と検証方法を計画します。

1. **ターゲットの選定**:
   `playtest_review.md` の中から、今回修正・実装する課題を絞り込みます。
2. **計画書の作成**:
   アーティファクトディレクトリ内に `implementation_plan.md` を作成または更新します。
   - **内容**: 変更の目的、影響を受けるファイル、具体的な修正アプローチ、テスト/検証計画。

---

### 💬 フェーズ 3: サブエージェントとの設計議論
目的: 計画段階でバグや設計の考慮漏れがないか、サブエージェントと議論して計画を研ぎ澄まします。

1. **サブエージェントの起動**:
   `define_subagent` で `design_reviewer`（設計レビュー担当）を定義し、`invoke_subagent` で呼び出します。
2. **レビュー依頼**:
   作成した `implementation_plan.md` を渡し、以下の点を議論します。
   - 「この変更により、既存のGameSessionやAIManagerの状態遷移に矛盾が生じないか？」
   - 「非同期オンライン（Cram/Friendモード）の同期データに悪影響を与えないか？」
   - 「よりシンプルで堅牢な別のアプローチはないか？」
3. **計画への反映**:
   議論を通じて得られた指摘をもとに、`implementation_plan.md` を更新・確定させます。

---

### 💻 フェーズ 4: コード案作成とサブエージェントとのコードレビュー
目的: 実際のコーディング前に、具体的なコードレベルでの正確性をサブエージェントと検証します。

1. **コード案の作成**:
   修正対象ファイルの具体的な GDScript 変更コード案（差分または関数単位）を作成します。
2. **サブエージェントでのコードレビュー**:
   `self` または定義した `code_reviewer` サブエージェントを起動し、作成したコード案をレビューさせます。
   - **確認項目**: GDScript 4.6 の静的型付けルールへの準拠、メモリリークの有無、エッジケース（Null参照、バースト0点時など）でのクラッシュリスク。
3. **コード案のブラッシュアップ**:
   サブエージェントの指摘を反映してコード案を修正し、最終的なコーディング用コードを確定させます。

---

### 🔧 フェーズ 5: コーディングと実機テスト検証
目的: 確定したコードを反映し、正しく動作することをテストで証明します。

1. **コード適用**:
   `replace_file_content` または `multi_replace_file_content` を用いて、対象ファイルにコードを適用します。
2. **ビルド更新 (Webエクスポート)**:
   Godotのエクスポートコマンドを実行し、`build/web` のHTML5ビルドファイルを更新します。
3. **検証テストの実行**:
   `python serve.py` を起動した状態で `node test_browser.js` を実行し、エラーログが出ていないこと、スクリーンショットで期待通りのUIが表示されていることを確認します。
4. **結果報告 (Walkthrough)**:
   アーティファクトディレクトリ内に `walkthrough.md` を作成または更新し、変更内容とテスト結果（スクリーンショット画像など）をまとめ、ユーザーへ報告します。
