# 『テスト勉強チキンレース』 テストプレイ評価＆詳細レビュー

自動化テストプレイ（Puppeteer を用いたゲーム開始からドロー、休憩までの操作シーケンス）を実行し、実際の挙動・描画状態をもとに詳細レビューを作成しました。

---

## 🟢 評価された長所 (手触りとデザインの一貫性)

1. **没入感の高いダイエジェティックUI**:
   勉強机の上に置かれた「自習ノート」に見立てた見開き画面、マーカー風のステータス表示、鉛筆や消しゴムといったアナログ感のある小物の配置など、ビジュアルコンセプトが非常に高い完成度で統一されています。
2. **滑らかなイージングとアニメーション**:
   カードが山札から引かれて手札に加わる際のフリップ（めくり）演出や、複数枚のカードが重ならないよう扇状に並ぶファンアニメーションが、もたつきなく非常にスムーズに動作しています。
3. **直感的な情報提示**:
   現在適用されているアイテム効果（消しゴムチャージ、蛍光ペンなど）がノートの最上部に付箋（バッジ）として一覧表示され、プレイヤーが戦況を一目で理解できるよう配慮されています。
4. **状態遷移の正確性**:
   カードドロー中のボタン非活性化制御や、バーストチェック、休憩（ストップ）を選択した際の時限終了ポップアップから次時限（1時限目から2時限目）へのシームレスな移行など、ゲームセッション管理は極めて頑健に実装されています。

---

## 🔴 検出された課題と改善提案

### 1. 【レイアウトバグ】生徒手帳（プロフィール）ダイアログの配置ズレ
- **現象**:
  `ProfileScene.gd` において、名前を入力する「生徒手帳の記帳」ダイアログ（`notebook_panel`）が画面中央ではなく、右下に大きく偏って配置されています（スケーリングの影響で左上が画面中央 `576, 324` に配置されている状態）。
- **原因**:
  `notebook_panel` の `pivot_offset` が設定されていない（デフォルトの `0, 0`）ため、Entranceアニメーションの座標計算や中央配置のオフセットが正しく引かれていません。
- **改善案**:
  `ProfileScene.gd` の `_ready()` 内で、サイズ決定直後にピボットを中央に設定するコードを追加します：
  ```gdscript
  notebook_panel.custom_minimum_size = Vector2(800, 500)
  notebook_panel.pivot_offset = notebook_panel.custom_minimum_size * 0.5
  ```

### 2. 【UI/UX】カード詳細ツールチップ（説明ポップアップ）のボタン被り
- **現象**:
  カードを引いた際に出現する「カード詳細説明」のツールチップ（`card_detail_box`）が、操作パネルの下部ボタン（「勉強カードを引く」「休憩する」）の上に重なって表示されています。
- **リスク**:
  カード効果のテキスト量が多かったり、画面の解像度が低かったりする場合に、プレイヤーがドロー/ストップボタンを押すのを物理的・視覚的に邪魔する原因となります。
- **改善案**:
  ツールチップの位置を、ボタンの上ではなく「左ページの空いているスペース（勉強成果と勉強履歴の間）」に固定配置にするか、あるいはホバーしているカードの真上（Y座標がより高い位置）に動的に追従させるように変更します。

### 3. 【QA・テスト性】自動テストの座標依存の高さ
- **現象**:
  外部ブラウザテストツール（Puppeteerなど）からゲーム内のボタンを操作する際、Godot が画面アスペクト比に応じて Canvas 全体を自動スケーリングするため、ピクセル座標の計算が極めて複雑になり、画面サイズ変更に脆弱です。
- **改善案**:
  Godot側からJavaScriptブリッジを介して、主要なUIボタン（ドロー、ストップ、決定など）の画面上でのリアルタイムバウンディングボックス（四角形座標）をHTML/JS側に通知する仕組みをデバッグ用に実装するか、キーボードのフォーカス遷移（TabやEnterキー）による進行に完全対応させることで、テストの堅牢性を高めます。
